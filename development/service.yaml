apiVersion: v1
kind: Service
metadata:
  name: argocd-demo-app1-service
spec:
  type: NodePort
  selector:
    app: argocd-demo-app1
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007